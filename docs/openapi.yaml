openapi: 3.1.0
info:
  title: LazyOrch API
  description: |
    HTTP API for the LazyOrch daemon.
    Placeholder — full paths and schemas land with the daemon implementation.
  version: 0.0.0
paths: {}
