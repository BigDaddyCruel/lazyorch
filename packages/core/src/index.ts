/**
 * @lazyorch/core — FSM, tasks, planning, scheduling, types.
 * Placeholder package; domain logic lands in later PRs.
 */
export const PACKAGE_NAME = "@lazyorch/core" as const;

export function corePlaceholder(): string {
  return PACKAGE_NAME;
}
