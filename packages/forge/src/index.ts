/**
 * @lazyorch/forge — git + github integration.
 * Placeholder package; domain logic lands in later PRs.
 */
export const PACKAGE_NAME = "@lazyorch/forge" as const;

export function forgePlaceholder(): string {
  return PACKAGE_NAME;
}
